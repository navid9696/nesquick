import React from 'react'
import { IMovie } from '../../lib/types'

const MovieCard = ({ movie }: { movie: IMovie }) => {
	return <div className='border border-solid border-red-400 my-5 cursor-pointer'>MovieCard</div>
}

export default MovieCard
